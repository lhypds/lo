PORT=3014
PM2_NAME=lo
VITE_MAPBOX_TOKEN=
# Server-side only — never prefix this with VITE_, or the bundler would ship
# your bearer token to the browser. X meters /2/trends/by/woeid per request, so
# the trends card is off until this is set. https://docs.x.com/x-api/trends
X_BEARER_TOKEN=
